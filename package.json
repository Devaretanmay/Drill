{
  "name": "drill",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered log diagnosis CLI",
  "license": "BUSL-1.1",
  "scripts": {
    "build": "pnpm --filter drill-cli build",
    "dev": "pnpm --filter drill-cli dev",
    "test": "pnpm --filter drill-cli test",
    "test:coverage": "pnpm --filter drill-cli test:coverage",
    "typecheck": "pnpm --filter drill-cli typecheck",
    "lint": "pnpm --filter drill-cli lint",
    "clean": "pnpm --filter drill-cli clean"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
