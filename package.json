{
  "name": "drill",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered log diagnosis CLI",
  "license": "BUSL-1.1",
  "scripts": {
    "build": "pnpm --filter drill-log build",
    "dev": "pnpm --filter drill-log dev",
    "test": "pnpm --filter drill-log test",
    "test:coverage": "pnpm --filter drill-log test:coverage",
    "typecheck": "pnpm --filter drill-log typecheck",
    "lint": "pnpm --filter drill-log lint",
    "clean": "pnpm --filter drill-log clean"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
